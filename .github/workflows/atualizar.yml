name: Atualizar METAR/TAF

on:
  schedule:
    - cron: '0 * * * *'  # corre de hora a hora
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Atualizar ficheiros JSON com base em config/icaos.csv
        run: |
          mkdir -p dados

          echo "🔄 A carregar ICAOs a partir de config/icaos.csv..."
          ICAOS=$(tail -n +2 config/icaos.csv | cut -d',' -f1)

          for ICAO in $ICAOS; do
            echo "🔄 A atualizar $ICAO..."

            METAR=$(curl -s "https://tgftp.nws.noaa.gov/data/observations/metar/stations/${ICAO}.TXT" | tail -n1 || true)
            TAF=$(curl -s "https://tgftp.nws.noaa.gov/data/forecasts/taf/stations/${ICAO}.TXT" | grep "^TAF" || true)

            [ -z "$METAR" ] && METAR="METAR não disponível para ${ICAO}"
            [ -z "$TAF" ] && TAF="TAF não disponível para ${ICAO}"

            TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

            echo "{\"icao\":\"$ICAO\",\"metar\":\"$METAR\",\"updated\":\"$TIMESTAMP\"}" > dados/metar-$ICAO.json
            echo "{\"icao\":\"$ICAO\",\"taf\":\"$TAF\",\"updated\":\"$TIMESTAMP\"}" > dados/taf-$ICAO.json
          done

          echo "🧩 Gerando lista de ICAOs..."
          ICAO_LIST=$(ls dados/metar-*.json | sed 's|dados/metar-||;s|\.json||' | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "$ICAO_LIST" > dados/icao-lista.json

          echo "🧩 Gerando mapa de nomes..."
          tail -n +2 config/icaos.csv | jq -Rn '
            [inputs | split(",")] | map({(.[0]): .[1]}) | add
          ' > dados/icao-nomes.json

      - name: Commit e push automático
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Atualização automática METAR/TAF + ICAOs dinâmicos"
          file_pattern: dados/*.json
